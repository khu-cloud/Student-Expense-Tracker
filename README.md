# Student-Expense-Tracker
A simple Python-based Student Expense Tracker that helps users record, view, search, calculate, and delete daily expenses. It stores data permanently in a JSON file and uses basic Python concepts like functions, lists, dictionaries, loops, file handling, and exception handling.
